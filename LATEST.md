# India IPO Magazine – Frontend

- Uses short‑lived tokens and a secure stream endpoint.
- Tailwind UI, Razorpay Checkout included, SPA redirects for Netlify.

## Run

```bash
npm install
echo "VITE_API_URL=http://localhost:5000/api" > .env
npm run dev
```

## Build

```bash
npm run build
```
